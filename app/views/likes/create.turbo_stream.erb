<turbo-stream action="replace" target="<%=dom_id(@comment, :like)%>">
  <template>
    <%= render 'students/likes_count', comment: @comment, student: @comment.student %> 
  </template>
</turbo-stream>